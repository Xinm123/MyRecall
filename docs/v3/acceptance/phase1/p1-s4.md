# P1-S4 验收记录（检索）

- 阶段：P1-S4
- 日期：2026-03-18（计划，执行窗口以 roadmap 为准）
- 负责人：TBD
- 版本/提交：TBD
- 状态：`Planned`

## 0. screenpipe 对照与对齐（020A）

- screenpipe 做法：`GET /search` 基于纯 FTS5，无 embedding 参与；`GET /search/keyword` 为独立快速路径。
- 本阶段对齐（020A）：线上检索仅 FTS5+过滤，不引入 hybrid；`/v1/search/keyword` 合并到 `/v1/search`（P1 无 embedding，拆分无意义）。
- v3 主线约束：检索按 OCR-only 单路径收口，不验证 `search_accessibility()` / `search_all()` / `content_type=accessibility/all`。

## 1. 范围与目标

- 范围：`/v1/search`（含原 keyword 路径，已合并）、返回字段契约与 UI 过滤交互。
  > **注意**：`/v1/search` 为 P1-S4 **新实现**端点（P1-S1~S3 阶段不存在）。
- 目标：确保检索准确、可过滤、可回溯，并验证 UI 与 API 参数映射一致。
- 对应 Gate 条件：
  - 观测 KPI：Search P95 记录实际分布（标准时间窗 <= 24h；详见 [../../gate_baseline.md#35-search-p95p1-s4](../../gate_baseline.md#35-search-p95p1-s4)）。
  - `/v1/search` 过滤参数契约完成率 = 100%。
  - OCR 搜索 SQL/返回结构一致性 = 100%。
  - FTS 清空一致性 = 100%。
  - OCR-only 可检索完整性一致性 = 100%。
  - `/v1/search/keyword` 不作为独立端点暴露（请求返回 `404 Not Found`）。
  - Search 引用字段完整率 = 100%（`frame_id`/`timestamp`，Hard Gate）。
- Search UI 过滤项契约映射完成率 = 100%。
  - 检索结果点击回溯成功率 >= 95%。

### 1.1 HTTP 契约 delta（本阶段，scope=对外 HTTP）

- SSOT：[../../http_contract_ledger.md](../../http_contract_ledger.md)

| 类型 | 接口 | 变化/说明 | SSOT |
|---|---|---|---|
| ADD | GET `/v1/search` | Search 对外可用（FTS5 + 过滤参数 + response schema；未实现保留字段如 browser_url/tags/include_frames 必须显式返回 null/[]，禁止直接省略 Key） | [../../spec.md](../../spec.md) §4.5 |
| CHANGE | GET `/v1/search/keyword` | P1 不提供独立端点：必须返回 `404 Not Found`（避免回归出独立路径） | 本文件 §1 Gate；[../../http_contract_ledger.md](../../http_contract_ledger.md) |
| REMOVE | `POST /api/upload`; `GET /api/search`; `GET /api/queue/status`; `GET /api/health` | P1-S4 起：统一 `410 Gone`（完全废弃，不再阶段性重定向） | [../../roadmap.md](../../roadmap.md) §1.1 |

### 1.2 Search UI 过滤项映射清单

| UI 过滤项 | API 参数 | P1 实现状态 | 说明 |
|-----------|----------|------------|------|
| 时间范围 | `start_time`/`end_time` | ✅ P0 | ISO8601 UTC |
| 应用名 | `app_name` | ✅ P0 | 精确匹配 |
| 窗口名 | `window_name` | ✅ P0 | 精确匹配 |
| 前台帧 | `focused` | ✅ P0 | bool |
| 文本长度 | `min_length`/`max_length` | ✅ P1 | uint，OCR 文本字符数 |
| browser_url | - | ❌ 不实现 | P1 reserved seam，固定 NULL，不作为过滤能力 |
| include_frames | - | ❌ 不实现 | P1 预留字段，始终返回 null |

> **映射验证方式**：在 Search 页面逐项操作过滤器，抓包核对 UI 参数与 API 请求 1:1 对应。

### 1.3 测试文件清单

| 文件 | 内容 | 验收目标 | 关键测试场景 |
|------|------|----------|--------------|
| `test_p1_s4_search_fts.py` | FTS 召回正确性（OCR-only，关键词命中） | 召回率 = 100% | 单词查询、短语查询、前缀匹配、空查询、特殊字符转义 |
| `test_p1_s4_sql_path.py` | SQL 路径验证（基于日志或松散 EXPLAIN 验证 JOIN 与 FTS 条件追加，防 flaky test） | 路径一致性 = 100% | 有 q 无过滤、有 q 有过滤、无 q 有过滤、无 q 无过滤 |
| `test_p1_s4_response_schema.py` | 响应结构兜底验证（未实现字段显式返回 null/[] 而非省略，防前端 NPE） | 契约一致性 = 100% | 成功响应字段完整性、空结果响应结构、错误响应格式 |
| `test_p1_s4_fts_clear_safe.py` | FTS 清空一致性（字段清空后 token 不再命中） | 清空一致性 = 100% | app_name 清空、window_name 清空、text 清空、多字段同时清空 |
| `test_p1_s4_v4_seam.py` | v4 seam 保护（accessibility 表空值验证） | unexpected_accessibility_rows = 0 | 表为空检查、历史残留清理验证 |
| `test_p1_s4_reference_fields.py` | 引用字段完整率（frame_id + timestamp 非空） | 完整率 = 100%（Hard Gate） | 单条结果验证、批量结果验证、边界值验证 |
| `test_p1_s4_ui_filter_mapping.py` | UI 过滤项映射（UI 参数 → API 参数 1:1） | 映射完成率 = 100% | 时间范围过滤、app_name 过滤、window_name 过滤、focused 过滤、组合过滤 |
| `test_p1_s4_citation_backtrace.py` | 回溯成功率（搜索结果 → frame/timeline） | 成功率 >= 95% | 点击跳转 timeline、frame_id 解析、timestamp 定位 |
| `test_p1_s4_legacy_api.py` | 旧 API 废弃验证（`/api/*` 返回 410 Gone） | 契约一致性 = 100% | 4 个 legacy 端点分别返回 410、响应体格式验证、日志无 DEPRECATED |

> **TDD 开发**：建议按上述顺序实现测试，后实现功能代码。

### 1.4 v4 seam 保护规则

| 检查项 | 类型 | 阈值 | 发现数据时的处理 |
|--------|------|------|-----------------|
| `unexpected_accessibility_rows` | Hard Gate | = 0 | 详见下方判定逻辑 |

**判定逻辑**：

若 `accessibility` 表有数据，首先判断来源：

1. **代码写入**（v3 代码路径意外写入）→ Hard Gate **Fail** + 代码修复 + 数据回滚
2. **历史残留**（P1-S2b 之前的 migration/测试数据）→ 清理后通过，记录为"seam 污染"

> **说明**：v4 seam 意味着"保留 schema 等 v4 来写"，不是"v3 可以写"。spec.md 明确写 `v3 代码完全不触碰此表`。

**具体判定流程（SQL + 决策树）**：

```sql
-- Step 1: 检查 accessibility 表总行数
SELECT COUNT(*) AS total_rows FROM accessibility;

-- Step 2: 若 total_rows > 0，检查是否有 frame_id 关联（识别代码写入特征）
SELECT 
    COUNT(*) AS rows_with_frame_id,
    COUNT(CASE WHEN frame_id IS NOT NULL THEN 1 END) AS non_null_frame_id
FROM accessibility;

-- Step 3: 检查时间分布（识别历史残留特征）
SELECT 
    MIN(timestamp) AS earliest_row,
    MAX(timestamp) AS latest_row,
    COUNT(*) AS total
FROM accessibility;
```

**决策规则**：

| 场景 | 判定标准 | 处理动作 | Gate 结果 |
|------|----------|----------|-----------|
| `total_rows = 0` | 表为空 | 无需处理 | ✅ **Pass** |
| `non_null_frame_id > 0` | 存在 frame_id 关联（代码写入特征） | 1. 定位写入代码路径<br>2. 修复代码<br>3. 回滚/删除数据<br>4. 重新执行验收 | ❌ **Fail** |
| `non_null_frame_id = 0` 且 `earliest_row < '2026-03-13'` | 无 frame_id 但存在 OQ-043 决议前的历史数据 | 1. 执行 `DELETE FROM accessibility`<br>2. 执行 `DELETE FROM accessibility_fts`<br>3. 验收记录中标注 "seam 污染已清理" | ✅ **Pass**（附注） |
| `non_null_frame_id = 0` 且 `earliest_row >= '2026-03-13'` | 无 frame_id 但日期在 OQ-043 后（异常） | 人工核查来源，按历史残留流程处理 | ⚠️ **人工判定** |

**清理后验证 SQL**：
```sql
-- 必须返回 0
SELECT COUNT(*) FROM accessibility;
SELECT COUNT(*) FROM accessibility_fts;
```

### 1.5 Entry Prerequisites（分阶段前置条件）

#### 1.5.1 开发启动条件（Dev Entry）

开始 P1-S4 代码开发前必须满足：

- [ ] P1-S3 Gate 结论为 `Pass`（验收记录：[`p1-s3.md`](./p1-s3.md)）
- [ ] S3 产出数据可验证：
  - `ocr_text` 表有数据（至少 1 条 completed 帧）
  - `ocr_text_fts` 索引已就绪（trigger_count >= 3）
  - FTS 触发器已激活
- [ ] 文档口径已冻结（roadmap/acceptance/gate_baseline/spec/data-model 一致）

#### 1.5.2 验收启动条件（Acceptance Entry）

开始 P1-S4 验收测试前必须满足：

- [ ] `/v1/search` 端点已实现并返回 HTTP 200
- [ ] 数据库样本数据量满足 §2.2 要求：
  - 总帧数 >= 100 条 completed 帧
  - 有 OCR 文本的帧数 >= 100 条
- [ ] 测试文件已实现并通过（参见 §1.3 测试清单）

### 1.6 S3 -> S4 Handoff Contract（Entry Contract）

- **SSOT**：[p1-s3.md §7.2](./p1-s3.md#72-s3---s4-数据流契约)
- **Ownership 边界**：S3 独占 OCR 执行与 `ocr_text` 写入；S4 仅消费 `ocr_text`、`frames.text_source` 和 `ocr_text_fts`。
- **数据一致性不变量（前置验证）**：

| 检查项 | SQL | 预期结果 |
|--------|-----|----------|
| `ocr_text` 与 `frames` 一致性 | `SELECT COUNT(*) FROM frames f LEFT JOIN ocr_text o ON o.frame_id=f.id WHERE f.status='completed' AND f.text_source='ocr' AND o.id IS NULL;` | = 0 |
| FTS 索引就绪 | `SELECT COUNT(*) FROM sqlite_master WHERE type='trigger' AND tbl_name='ocr_text';` | >= 3 |
| 最小数据量 | `SELECT COUNT(*) FROM ocr_text;` | >= 1 |

- **FTS 索引就绪**：OCR 成功后，FTS 触发器自动写入 `ocr_text_fts`；S4 搜索依赖该索引就绪。
- **验证时机**：在 S4 开发启动前执行上述 SQL，确认 S3 产出符合预期。

## 2. 环境与输入

- 运行环境：同 P1-S1。
- 配置与数据集：
  - 搜索查询样本集（>= 200 条，覆盖不同过滤组合）
  - 多过滤条件组合集（time/app/window/focused；`browser_url` 仅保留兼容参数样本）
- 依赖版本：search API 与索引版本

### 2.1 样本就绪检查清单（验收前必检）

| 检查项 | 最小阈值 | 验证 SQL | 达标状态 |
|--------|----------|----------|----------|
| 数据库总帧数 | >= 100,000 | `SELECT COUNT(*) FROM frames;` | [ ] |
| completed 帧数 | >= 100 | `SELECT COUNT(*) FROM frames WHERE status='completed';` | [ ] |
| 有 OCR 文本帧数 | >= 100 | `SELECT COUNT(*) FROM ocr_text;` | [ ] |
| FTS 触发器数量 | >= 3 | `SELECT COUNT(*) FROM sqlite_master WHERE type='trigger' AND tbl_name IN ('frames', 'ocr_text');` | [ ] |
| accessibility 表为空 | = 0 | `SELECT COUNT(*) FROM accessibility;` | [ ] |

> **判定规则**：上述检查项全部达标后，方可开始 P1-S4 验收测试。

### 2.2 指标口径与样本说明（必填）

- 口径基线版本（默认 [../../gate_baseline.md](../../gate_baseline.md)）：v1.7
- 指标样本数：
  - **数据库基底数据量**：>= 100,000 条记录（模拟 1~3 天真实积累，确保 P95 反映真实规模下的 FTS 性能；低于此规模的 P95 无效）
  - Search 查询样本：>= 200 queries（低于该值不得判定 `Search P95`）
  - 过滤组合测试样本：>= 100 条
  - FTS clear-safe 回归样本：每类字段清空场景至少 20 条
- 统计时间窗：
  - 主窗：标准时间窗（查询窗口 <= 24h）
  - 回归窗：每次 SQL/触发器调整后重跑 1 轮
- 百分位算法：Nearest-rank（剔除前 10 个预热样本）

## 3. 验收步骤

1. 导入标准检索数据集，记录基线版本。
2. 运行搜索查询样本集，记录 Search P95 延迟分布。
3. 对 `/v1/search` 执行过滤组合测试（单参、多参、边界时间窗）。
4. 通过 SQL 日志或 `EXPLAIN QUERY PLAN` 抽样核对单路径 OCR 查询：
   - 主路径 `frames INNER JOIN ocr_text`
   - `frames_fts` 仅在 app/window/focused 非空时追加；`browser_url` 在 P1 不作为 active 过滤能力 Gate
   - `ocr_text_fts` 仅在 `q` 非空时追加
   - **注意**：自动化断言必须使用松散关键字（如要求存在 `USING FTS5 SEARCH`、避免 `SCAN TABLE`），禁止与特定的 SQLite 内部查询计划文本强绑定。
5. 执行 OCR-only 一致性核对 SQL：
   - `missing_ocr`：
     ```sql
     SELECT COUNT(*) AS missing_ocr
     FROM frames f
     LEFT JOIN ocr_text o ON o.frame_id = f.id
     WHERE f.status='completed' AND f.text_source='ocr' AND o.frame_id IS NULL;
     ```
   - `unexpected_accessibility_rows`（v4 seam 保护检查）：
     ```sql
     SELECT COUNT(*) AS unexpected_accessibility_rows
     FROM accessibility;
     ```
     > **检查目的**：验证 v4 reserved seam 未被 v3 代码意外写入。OCR-only 主线下该值应为 0。
    - `orphan_ocr`（反向检查）：
      ```sql
      SELECT COUNT(*) AS orphan_ocr
      FROM ocr_text ot
      LEFT JOIN frames f ON f.id = ot.frame_id
      WHERE f.id IS NULL;
      ```
      > **检查目的**：验证 `ocr_text` 表无孤儿记录（即不存在 frame_id 指向不存在的 frame）。
    判定规则：`missing_ocr=0 AND unexpected_accessibility_rows=0 AND orphan_ocr=0`。
6. 验证 `/v1/search` 的关键词路径性能（`/v1/search/keyword` 已合并，P1 不作为独立端点）。
7. 对 `GET /v1/search/keyword` 发起请求，验证返回 `404 Not Found`（避免独立端点回归）。
8. 旧命名空间废弃检查：
   > **scope 说明**：P1 Gate 仅覆盖以下 4 个 legacy 端点；其余 `/api/*` 端点不作为验收范围（参见 [http_contract_ledger.md](../../http_contract_ledger.md) §4.0）。
   - 对以下 4 个 legacy 端点分别发起请求：
     - `POST /api/upload`
     - `GET /api/search`
     - `GET /api/queue/status`
     - `GET /api/health`
   - 验证**统一返回 `410 Gone`**（不再是 301/308 重定向）
   - 验证响应体符合 spec.md §4.9 统一错误格式：
     ```json
     {
       "error": "gone",
       "message": "This API endpoint has been removed"
     }
     ```
   - 验证日志中**不再出现** `[DEPRECATED]`（410 后不再记录废弃提示）
   - 记录响应样本与日志证据。

> **注意**：P1-S3 阶段 `/api/search` → 301 → `/v1/search`（目标端点可能 404，属预期）；P1-S4 阶段必须全部改为 410。
9. 抽样校验引用字段完整性：
   - 搜索结果中的 `frame_id` 与 `timestamp` 同时非空。
10. 在 Search 页面逐项操作过滤器，抓包核对 UI 参数与 API 请求 1:1 对应。
11. 从检索结果点击回溯到 frame/timeline，统计成功率。
12. 执行 FTS clear-safe 回归（`frames_fts` + `ocr_text_fts`）：
   - 构造样本（包含 `app_name/window_name/focused/text` 的可检索 token；`browser_url` 仅验证 clear-safe reserved 列不产生脏索引）。
   - 对样本行执行 UPDATE，将上述字段改为 `NULL` 或 `''`。
   - 立即执行对应 MATCH 查询，验证旧 token 命中数 = 0。
   - 再次写入新 token 并查询，验证新 token 可命中。

## 4. 结果与指标

### 4.1 数值指标（可放宽，但需达标）

- Search P95（**仅观测记录实际分布，P1-S4 不设硬性阈值，超标不阻塞 Gate**；详见 [../../gate_baseline.md#35-search-p95p1-s4](../../gate_baseline.md#35-search-p95p1-s4)）：
- OCR 搜索 SQL/返回结构一致性（目标 = 100%）：
- FTS 清空一致性（目标 = 100%）：
- OCR-only 可检索完整性一致性（目标 = 100%）：
- Search 引用字段完整率（目标 = 100%，Hard Gate）：
- missing_ocr / unexpected_accessibility_rows / orphan_ocr（目标 = 0）：
- 结果点击回溯成功率（目标 >= 95%）：
- 备注（是否满足最小样本数要求）：是 | 否（不足项：...）

### 4.2 功能完成度指标（强制）

- 功能清单完成率（目标 100%）：
- API/Schema 契约完成率（目标 100%）：
- API 命名空间一致性（对外仅 `/v1/*`；P1-S4 起 legacy 端点 `POST /api/upload`、`GET /api/search`、`GET /api/queue/status`、`GET /api/health` 返回 `410 Gone`，目标 100%）：
- 关键功能用例通过率（目标 >= 95%）：

### 4.3 完善度指标（强制）

- 异常与降级场景通过率（目标 >= 95%）：
- 可观测性检查项完成率（目标 100%，日志/指标/错误码）：
- 文档与验收记录完整率（目标 100%）：

### 4.4 UI 验收（按阶段启用）

- 路由可达与基础状态可见性检查（健康态/错误态）：通过。
- UI 关键交互通过率（如 timeline/search/chat/citation）：本阶段检查 Search 过滤 + 结果回溯。
- UI 证据附件（截图/录屏/日志路径）：

## 5. 结论

- Gate 结论：`Pass` | `Fail`
- Fail-fast 规则：任一 Hard Gate（Search 引用字段完整率）未达到 100%，或 `missing_ocr > 0` / `unexpected_accessibility_rows > 0` / `orphan_ocr > 0`，本阶段必须判定为 `Fail`。
- 样本数符合性判定：`Pass` | `Fail`（若 `Fail`，本阶段不得给出 Gate `Pass` 结论）
- 依据：
- 阻塞项（若 Fail 必填）：

## 6. 风险与后续动作

- 风险：语义类查询退化可能影响后续 chat grounding。
- 后续动作：将低召回问题样本转入 Chat 阶段专项回归。
