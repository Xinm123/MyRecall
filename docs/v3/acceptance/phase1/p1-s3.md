# P1-S3 验收记录（OCR Processing）

- 阶段：P1-S3
- 日期：2026-03-15（计划，执行窗口以 roadmap 为准）
- 负责人：TBD
- 版本/提交：TBD
- 状态：`Planned`

## 0. screenpipe 对照与对齐

- screenpipe 做法：vision-only 处理链在可用时生成 OCR/raw text，并避免索引时 AI 增强。
- 本阶段对齐：v3 主线只实现 OCR processing，不实现 AX/dual-path 决策逻辑，不把 `accessibility` 表作为活跃写入目标。
- P1 OCR 引擎策略：RapidOCR（single-engine policy），本阶段不做多引擎切换或对比验收。
- 零 AI 增强（OQ-014/OQ-015）：P1 不生成 `caption/keywords/fusion_text`，不写入 `ocr_text_embeddings`；本阶段做防守性确认。

## 1. 范围与目标

- 范围：Edge OCR 处理链路、`ocr_text` 持久化、`frames.text_source='ocr'`、失败帧 `failed` 语义、处理来源可解释性、**UI 处理来源展示**
- **UI 实现要求**：P1-S3 必须完成 Grid `/` 的 frame 卡片显示（详见 §3.9）：
  - Card Header：app + window + device_name
  - Card Footer 四行：trigger+timestamp / OCR引擎+processed_at / 字符数 / OCR文本预览(50字符，点击展开100)
  - 状态显示：PROCESSING / PENDING / COMPLETED(OCR成功/失败/无OCR) 各自对应显示逻辑
  - 详见 roadmap.md line 169
  - ⚠️ **此为实现要求，非可选功能，不可跳过**
- 目标：验证 OCR-only 处理主线正确、可追溯、可恢复，**且处理结果在 UI 可见**
- 对应 Gate 条件：
  - OCR 成功帧写入 `ocr_text` 的正确率 = 100%（硬门槛）
  - `frames.text_source='ocr'` 标记正确率 = 100%
  - OCR 失败帧 `failed` 语义正确率 = 100%
  - 索引时零 AI 增强检查通过率 = 100%
  - 处理来源字段 UI 展示完整率 = 100%（硬性交付物，不可跳过）

### 1.1 HTTP 契约 delta（本阶段，scope=对外 HTTP）

- SSOT：[../../http_contract_ledger.md](../../http_contract_ledger.md)

| 类型 | 接口 | 变化/说明 | SSOT |
|---|---|---|---|
| CHANGE | `/v1/ingest/queue/status` | `processing_mode` 语义变更：P1-S1 为 `"noop"` → P1-S3+ 为 `"ocr"`（启用 OCR 处理） | [spec.md §4.7] |
| RETAIN | 其他 `/v1/*` | 对外 HTTP 无新增/废弃/替代端点 | [spec.md §4.9] |

## 2. 环境与输入

- 运行环境：同 P1-S1。
- 配置与数据集：
  - 普通 OCR 场景样本
  - OCR 高负载样本（大图 / 高分辨率）
  - OCR 失败样本（输入损坏、超时、不可识别）
- 依赖版本：RapidOCR 引擎与处理 pipeline 版本

### 2.1 S2b -> S3 数据流契约（Entry Contract，强制）

- Ownership 边界：S2b 只负责 capture completion；S3 独占 OCR 执行、`ocr_text` 写入、`frames.text_source='ocr'` 标记与失败语义。
- 输入契约：S3 消费截图、`capture_trigger`、`device_name`、`focused_context`（即 `app_name` + `window_name`）、`event_ts`、`timestamp` 等 capture-completion 产物；不得要求 `accessibility_text` / `content_hash` 作为主线前提。
- `capture_trigger` 契约：
  - 合法值：凡由 S2b 产出的 capture，`capture_trigger` 必须为非空且属于 P1 枚举（`idle` / `app_switch` / `manual` / `click`）
  - **非法值处理流程**（fail-loud）：
    1. OCR 执行前进行 `capture_trigger` 校验
    2. 若发现 `NULL` 或非法值，立即设置 `frames.status='failed'`
    3. 设置 `frames.error_message='INVALID_TRIGGER: {actual_value}'`
    4. 输出结构化日志：`MRV3 ocr_failed frame_id=X reason=invalid_trigger value=Y`
    5. **不得执行 OCR**
- 上下文边界：S3 直接消费 S2b 已冻结的 `app_name/window_name/device_name`；`browser_url` 在 P1 保持 reserved/NULL，不得在处理阶段补采或覆盖。
  - **验证方法**：
    - 代码审查：确认 processing worker 不调用 `get_active_window()` 等补采 API
    - 验收 SQL：`SELECT COUNT(*) FROM frames WHERE processed_at IS NOT NULL AND (app_name NOT IN (SELECT app_name FROM frames WHERE id = frames.id) OR window_name NOT IN (SELECT window_name FROM frames WHERE id = frames.id))` 应为 0
- 写入契约：
  - OCR 成功 -> 必须存在 `ocr_text(frame_id=frames.id)`，且 `frames.text_source='ocr'`
  - OCR 失败 -> `frames.status='failed'`，不得伪造 `text_source='accessibility'` 或 accessibility child rows
- v4 seam 边界：`accessibility` / `accessibility_fts` / `frames.accessibility_text` 为 schema seam，不参与本阶段 Gate。
- 幂等契约：同一 `capture_id/frame_id` 的重试必须收敛到同一终态。
  - **终态冲突定义**：同一 `frame_id` 的已处理记录与新处理结果不一致（如第一次 OCR 成功，第二次重试 OCR 失败；或两次 OCR 成功但 `text` 内容不一致）
  - **两层防御策略**：
    1. **处理前检查**（第一层，优先策略）：
       - 查询 `frames` 表，若 `status='completed'` 且 `ocr_text` 已存在，直接跳过处理
       - 若 `status='failed'` 且 `retry_count >= 3`，直接跳过处理
       - 目的：避免浪费 OCR 计算资源
    2. **写入时检查**（第二层，最后防线）：
       - 写入 `ocr_text` 前再次检查是否已存在记录
       - 若发现冲突，**拒绝第二次写入**，保持第一次结果
       - 输出错误日志：`MRV3 idempotency_violation frame_id=X existing_status=Y new_status=Z`
       - 触发告警（用于排查为何第一层检查失效或发现引擎非确定性问题）

### 2.2 指标口径与样本说明（必填）

- 口径基线版本（默认 [../../gate_baseline.md](../../gate_baseline.md)）：v1.7
- OCR 运行时约束（P1）：`ocr_engine_runtime = rapidocr`
- 指标样本数：
  - 处理链路样本：>= 100 条处理记录
  - OCR 失败样本：每类异常至少 3 轮
- **样本符合性判定规则**：
  - 样本数达标 → §4.1 备注填写 "是"
  - 样本数不足 → §4.1 备注填写 "否（不足项：...）"，且 §5 不得给出 Gate `Pass`
- 统计时间窗：
  - 主窗：单次完整处理验收窗口
  - 异常窗：每类异常独立记录起止时间
- 百分位算法：Nearest-rank（若统计百分位，剔除前 10 个预热样本）

## 3. 验收步骤

1. 准备 OCR 成功与 OCR 失败两组样本数据。
2. 触发处理流程并记录每条 capture 的 OCR 执行结果、错误码与完成时间。
3. 用 SQL 校验 OCR 成功帧写入正确性：
   ```sql
   SELECT COUNT(*) AS missing_ocr
   FROM frames f
   LEFT JOIN ocr_text ot ON ot.frame_id = f.id
   WHERE f.status='completed'
     AND f.text_source='ocr'
     AND ot.id IS NULL;
   ```
   判定规则：`missing_ocr = 0`。
4. 用 SQL 校验失败语义：
   ```sql
   SELECT COUNT(*) AS failed_with_ocr_row
   FROM frames f
   JOIN ocr_text ot ON ot.frame_id = f.id
   WHERE f.status='failed';
   ```
   判定规则：`failed_with_ocr_row = 0`。
5. 用 SQL 校验 v4 seam 未被误写：
   ```sql
   SELECT COUNT(*) AS unexpected_accessibility_rows
   FROM accessibility;
   ```
   判定规则：`unexpected_accessibility_rows = 0`。
   > **检查目的**：验证 v4 reserved seam (`accessibility` 表) 未被 v3 代码意外写入。OCR-only 主线下该值应为 0。
6. 确认零 AI 增强（防守性检查）：
   ```sql
    -- 确认 ocr_text_embeddings 表不存在（OQ-015，P1 不建，防守性检查）
   SELECT COUNT(*) AS table_exists
   FROM sqlite_master
   WHERE type='table' AND name='ocr_text_embeddings';
   ```
   判定规则：`table_exists = 0`。
   > **检查目的**：防守性验证，确保未提前实现 embedding 表（OQ-014/OQ-015）。
7. 抽样核验至少 100 条处理记录，计算 OCR 处理来源可追溯率。
8. 边界用例核验（每类至少 3 条样本）：
   - OCR 成功后必须写 `ocr_text` 并标记 `text_source='ocr'`
   - OCR 失败后必须进入 `failed`
   - OCR 处理不得覆盖 S2b 已冻结的 `device_name` / `focused_context`
9. 在 Grid `/` 的 frame 卡片下方检查处理来源字段与处理时间戳展示完整性。
    - **验收标准**（全部为硬性要求，任一项不满足 = S3 Fail）：
   - **Card Header 布局**：
     - DOM 选择器：`.card-header`
     - 显示内容：app_name + window_name (左) + device_name (右)
     - 图标：🖥️ (app) + 📱 (device)
   - **Card Footer 布局**（四行，OCR 完成后）：
     - 第1行 (`.card-footer-row-1`)：`{trigger图标} {trigger} · ⏱️ {timestamp}`
       - trigger 图标：⏰ (idle), 🔄 (app_switch), 🖱️ (click), ✏️ (manual)
     - 第2行 (`.card-footer-row-2`)：`{text_source图标} OCR ({ocr_engine}) · ✓ {processed_at}`
       - text_source 图标：✨ (ocr成功), ❌ (ocr失败)
     - 第3行 (`.card-footer-row-3`)：`📝 {text_length} chars`
     - 第4行 (`.card-footer-row-4`)：OCR 文本预览（前50字符，点击展开至100字符）
    - **状态显示逻辑**（基于 `frames.status`，与 §2.1 写入契约对齐）：
      | 状态 | Card Header | Card Footer 第1行 | Card Footer 第2行 | Card Footer 第3-4行 |
      |------|-------------|-------------------|-------------------|---------------------|
      | `pending` | 显示 | ⏰ Pending... | 隐藏 | 隐藏 |
      | `processing` | 显示 | ⏳ Processing... | 隐藏 | 隐藏 |
      | `completed`（OCR成功） | 显示 | ✅ trigger + timestamp | ✨ OCR ({ocr_engine}) + processed_at | 📝 {text_length} chars + 文本预览 |
      | `failed`（OCR失败） | 显示 | ❌ trigger + timestamp | ❌ Failed + error_message | 显示 `--` |

    - **data 属性**：`data-frame-status`（与 `frames.status` 对齐）
      - 值枚举：`pending` / `processing` / `completed` / `failed`
      - 用途：自动化测试锚点

    - **判定规则**（分场景，避免"100%存在率"与"--"矛盾）：

      **场景 A：OCR 成功帧（主场景）**
      - 抽样 100 条 `status='completed'` 且 `ocr_text` 存在的记录
      - Card Header 内容存在率 = 100%
      - Card Footer 第1-4行内容存在率 = 100%

      **场景 B：OCR 失败帧**
      - 抽样 20 条 `status='failed'` 的记录
      - Card Header 内容存在率 = 100%
      - Card Footer 第1-2行内容存在率 = 100%
      - 第3-4行显示 `--` 算合规（非空状态标签）

      **场景 C：处理中帧**
      - 抽样 10 条 `status='pending'` 或 `status='processing'` 的记录
      - Card Header 内容存在率 = 100%
      - 第1行显示状态标签（⏰ Pending / ⏳ Processing）算合规
      - 第2-4行隐藏或显示占位符算合规

    - **自动化验证（推荐）**：
      ```bash
      # 方式1：Playwright 自动化测试
      pytest tests/e2e/test_p1_s3_ui.py -v

      # 方式2：人工抽检（无自动化时）
      # 按上述 A/B/C 场景各抽样，人工核对表格中的"显示"项
      ```

    - **注意**：UI 展示为硬性交付物，任一场景不达标 = S3 Fail
10. 注入处理异常（OCR 超时 / OCR 失败）并验证错误码、重试与降级日志。

## 4. 结果与指标

### 4.1 数值指标汇总（执行步骤见§3）

执行§3的验收步骤后，在此汇总结果。

#### Hard Gate 指标（任一项不达标 = S3 Fail）

| 指标 | 目标 | 实际值 | 验证步骤 | 达标状态 |
|------|------|--------|----------|----------|
| OCR成功帧写入`ocr_text`正确率 | 100% | ____% | §3.3 SQL | ☐ |
| `frames.text_source='ocr'`标记正确率 | 100% | ____% | §3.3 SQL | ☐ |
| OCR失败帧`failed`语义正确率 | 100% | ____% | §3.4 SQL | ☐ |
| OCR引擎运行时一致性 | 100% | ____% | §3.7 抽样 | ☐ |
| 零AI增强确认（防守性） | Pass | ☐Pass ☐Fail | §3.6 SQL | ☐ |
| 处理来源字段UI展示完整率 | 100% | ____% | §3.9 UI | ☐ |

#### 观测指标（non-blocking）

| 指标 | 参考阈值 | 实际值 | 备注 |
|------|----------|--------|------|
| OCR处理耗时P95 | ≤ 10s | ____s | 若>10s，分析分辨率/CPU/队列 |

#### 样本符合性（前置条件）

- [ ] 处理链路样本 >= 100 条（实际：____条）
- [ ] OCR失败样本 >= 3类（实际：____类，每类____条）
- [ ] 异常样本覆盖：success/failed/timeout/损坏（实际：____类）

**样本数判定**：若任一项不满足，§5 Gate结论必须为Fail

### 4.2 功能完成度指标（强制）

- 功能清单完成率（目标 100%）：
- API/Schema 契约完成率（目标 100%）：
- 关键功能用例通过率（目标 >= 95%）：

### 4.3 完善度指标（强制）

- processing-side 异常与降级场景通过率（目标 >= 95%）：覆盖 OCR success / OCR failure / failed 语义 / retry 收敛。
- 可观测性检查项完成率（目标 100%，日志/指标/错误码）：
- 文档与验收记录完整率（目标 100%）：

### 4.4 UI 验收（按阶段启用）

- 路由可达与基础状态可见性检查（健康态/错误态）：通过。
- UI 关键交互通过率（如 timeline/search/chat/citation）：本阶段检查 frame 详情“处理来源可见”通过率 = 100%。
- UI 证据附件（截图/录屏/日志路径）：

## 5. 结论

- Gate 结论：`Pass` | `Fail`
- Fail-fast 规则：若 `missing_ocr > 0`、`failed_with_ocr_row > 0` 或 `unexpected_accessibility_rows > 0`，本阶段必须判定为 `Fail`。
- 样本数符合性判定：`Pass` | `Fail`（若 `Fail`，本阶段不得给出 Gate `Pass` 结论）
- 依据：
- 阻塞项（若 Fail 必填）：

## 6. 风险与后续动作

- 风险：高分辨率场景下 OCR 处理耗时可能抬高后续 TTS 指标。
- 风险：错误重试或幂等收敛不当会导致 `ocr_text` 重复写入或终态漂移。
- 后续动作：若 OCR P95 超预期，优先分析分辨率分布、CPU 负载与队列深度，再决定是否进入 P2 优化项。

## 7. Exit Gate 与后续阶段

### 7.1 Exit Gate（允许进入 P1-S4）

满足以下全部条件方可退出 P1-S3：

- 本文档 §5 Gate 结论为 `Pass`
- 所有 Hard Gate 指标达标：
  - OCR 成功帧写入 `ocr_text` 正确率 = 100%
  - `frames.text_source='ocr'` 标记正确率 = 100%
  - OCR 失败帧 `failed` 语义正确率 = 100%
  - 索引时零 AI 增强检查通过率 = 100%
  - 处理来源字段 UI 展示完整率 = 100%
- 样本数符合性判定为 `Pass`（≥100 条处理记录，≥3 轮失败样本）
- 验收证据齐全（日志、指标汇总、SQL 校验结果、UI 证据）

### 7.2 S3 -> S4 数据流契约（Exit Contract）

- Ownership 边界：S3 独占 OCR 执行与 `ocr_text` 写入；S4 仅消费 `ocr_text`、`frames.text_source` 和 `ocr_text_fts`。
- **数据一致性不变量**：
  - `ocr_text` 行数 MUST EQUAL `frames.status='completed' AND frames.text_source='ocr'` 行数
  - `ocr_text_fts` 索引行数 MUST EQUAL `ocr_text` 行数（FTS 触发器自动维护）
- **FTS 索引就绪**：OCR 成功后，FTS 触发器自动写入 `ocr_text_fts`；S4 搜索依赖该索引就绪
- S4 Entry 前置条件：S3 必须产出至少 1 条 `text_source='ocr'` 的 completed 帧（空库场景 S4 无法验证搜索能力）

### 7.3 后续阶段路径

从 P1-S3 Exit Gate 后，进入：

- **P1-S4（检索能力）** — 主线必经
  - 说明：OCR-only 搜索路径依赖 S3 产出的 `ocr_text`
  - 依赖：S3 OCR 处理完成 + FTS 索引就绪
