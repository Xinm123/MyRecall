# P1-S4 UI Evidence Collection

## Evidence Captured: 2026-03-18

### UI Screenshots

1. **p1-s4-evidence-search-filters.png** - Search page showing all filter controls:
   - Search Query
   - Start Time / End Time
   - App Name
   - Window Name
   - Focused (dropdown: Any/Yes/No)
   - Min Text Length / Max Text Length

2. **p1-s4-evidence-search-results.png** - Search results page showing:
   - Filter values applied: q=test, app_name=iTerm2, min_length=100
   - 217 total results with pagination (Page 1 of 11)
   - Results display with: app name, timestamp, screenshot preview, rank score
   - Each result shows: frame_id, timestamp, device_name, window_name

### API Request/Response Evidence

**Request:**
```
GET /v1/search?q=test&app_name=iTerm2&min_length=100&limit=20&offset=0
```

**Response Status:** 200 OK

**Response Headers:**
- content-type: application/json
- content-length: 76240

**Response Structure Verified:**
- ✓ `type`: "OCR" (constant)
- ✓ `content.frame_id`: integer (e.g., 2701)
- ✓ `content.timestamp`: ISO8601 format (e.g., "2026-03-18T09:07:19.048000Z")
- ✓ `content.text`: string (OCR text content)
- ✓ `content.file_path`: string (screenshot filename)
- ✓ `content.frame_url`: string (API URL to frame)
- ✓ `content.app_name`: string (e.g., "iTerm2")
- ✓ `content.window_name`: string
- ✓ `content.browser_url`: null (reserved field)
- ✓ `content.focused`: null or boolean
- ✓ `content.device_name`: string (e.g., "monitor_1")
- ✓ `content.tags`: [] (empty array, reserved field)
- ✓ `content.fts_rank`: float (FTS relevance score)
- ✓ pagination with limit/offset/total

**Filter Mapping Verified:**
- ✓ Search Query → `q` parameter
- ✓ App Name → `app_name` parameter
- ✓ Min Text Length → `min_length` parameter
- ✓ All filter values correctly passed to API

---
Generated by automated test collection
